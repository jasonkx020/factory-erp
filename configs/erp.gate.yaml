# 本地生产倾向冒烟配置（勿提交真实密钥到生产；本文件供 gate 脚本使用）
seed:
  demo: false
server:
  addr: ":18080"
database:
  driver: sqlite
  sqlite_path: data/erp_gate.db
jwt:
  secret: "gate-smoke-strong-secret-32b"
  access_ttl_min: 120
  refresh_ttl_min: 10080
cors:
  allow_origins:
    - "http://127.0.0.1:5173"
    - "http://localhost:5173"
trace:
  hmac_secret: "gate-trace-hmac-strong-secret"
mqtt:
  enabled: false
