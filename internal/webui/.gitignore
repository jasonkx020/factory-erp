# Only ignore Vite build artifacts synced by release scripts.
# Keep placeholder *.html so `go:embed all:dist` always compiles.
dist/assets/
dist/admin/assets/
dist/front/boss/assets/
dist/**/*.js
dist/**/*.css
dist/**/*.map
